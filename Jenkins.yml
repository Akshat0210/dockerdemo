---
pipelineType: maven
git:
  project: CODE
  repo: dockerdemo
  appName: sampledemo
  tagDeploys: false
maven:
  command:
    build: "clean package"
build:
  branches:
    develop:
      deploy: true
